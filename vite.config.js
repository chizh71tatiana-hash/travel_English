import { defineConfig } from 'vite';

// Конфиг Vite для GitHub Pages
export default defineConfig({
// root можно не указывать, по умолчанию '.'
base: '/travel_English/', // ВАЖНО: имя репозитория c учётом регистра
});